name: Hello World!

on:
  push:
    branches:
      - setup-github-actions

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: |
          echo "Hello World!"
          date 
          ls
